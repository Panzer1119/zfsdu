Screen {
    layout: vertical;
}

#body {
    height: 1fr;
}

#browser {
    width: 1fr;
}

#path {
    border: round $accent;
    padding: 0 1;
    margin-bottom: 1;
    height: 3;
}

#entries {
    width: 1fr;
    height: 1fr;
    border: round $accent;
}

#details {
    width: 1fr;
    border: round $primary;
    padding: 1 2;
    overflow-y: auto;
}

#search-box {
    dock: bottom;
    height: auto;
    margin: 0 1 1 1;
}

#search-box.hidden {
    display: none;
}

#status {
    dock: bottom;
    height: 1;
    content-align: center middle;
    color: $text-muted;
}

